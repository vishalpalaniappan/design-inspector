// Store thunks here with engine to cleanly apply changes to the engine,
// update state and then make the relevant information available.

export const addParticipant = (engine, participant) => (dispatch, getState) => {


};
