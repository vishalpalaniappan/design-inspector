import {createSlice} from "@reduxjs/toolkit";

const scriptingSlice = createSlice({
    name: "scripting",
    initialState: {
    },
    reducers: {

    },
});

export const {} = scriptingSlice.actions;

export default scriptingSlice.reducer;
