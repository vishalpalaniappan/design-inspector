// Selectors for debugging slice
// This will be used to select the current behavior being inspected in the trace

// The UI will also visualize the root cause of the failed behavior.
// If the root cause isn't identified, then in the debug mode, the user will
// have the ability to add an invariant to a behavior in "learning" mode, this
// will then associate that invariant with the trace and it will be used for
// testing that the design's behavior respects this invariant.
