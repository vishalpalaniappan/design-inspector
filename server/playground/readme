This folder serves as a playground for the implementation contained in the engine. When the engine is opened, it will load the the files into the playground and continue to update it as the file is saved. This means that the user can run the program that was added to the engine. This is preparing for the next stage where the instrumented program will be executed in the playground and then the resulting execution trace will be saved in the engine.